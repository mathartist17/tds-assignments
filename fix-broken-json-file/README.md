## Notes

- Run the following command to check for json formatting issues
`python -c "import json; json.load(open('broken.json'))"`
- Fix the issue in VS code
- Rerun the command till the command does not throw any more error
- Submit the fixed json in the portal
